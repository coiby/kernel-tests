summary: 'FIXME: Raid setup'
description: |
    This case is desinged to test vm tunable min_free_kbytes.
    This tune is used to force the Linux VM to keep a minimum number
    of kilobytes free.  The VM uses this number to compute a
    watermark[WMARK_MIN] value for each lowmem zone in the system.
    Each lowmem zone gets a number of reserved free pages based
    proportionally on its size.

    Some minimal amount of memory is needed to satisfy PF_MEMALLOC
    allocations; if you set this to lower than 1024KB, your system will
    become subtly broken, and prone to deadlock under high loads.

    Setting this too high will OOM your machine instantly.
contact: Zhouping Liu <zliu@redhat.com>
id: 02b70074-68db-4142-b5fe-9a5336f9867b
component:
  - vm
test: bash ./min_free_kbytes.sh
framework: shell
recommend:
  - vm
tier: 1
tag: [tier1]
duration: 30m
extra-summary: /kernel/vm/sysctl/min_free_kbytes
extra-task: /kernel/vm/sysctl/min_free_kbytes
